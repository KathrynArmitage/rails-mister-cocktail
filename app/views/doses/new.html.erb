<%= simple_form_for [ @cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.input :ingredient_id, collection: Ingredient.all %>
  <%= f.submit "Update Cocktail", class: "btn btn-primary" %>
<% end %>
  <%=link_to 'Back to Cocktail Homepage', cocktails_path %>
